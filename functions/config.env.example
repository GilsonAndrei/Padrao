# Configurações de Ambiente
# Copie para .env e ajuste os valores

# Ambiente: development, production, staging
NODE_ENV=development

# Domínios de email permitidos para administradores (separados por vírgula)
ALLOWED_DOMAINS=@empresa.com,@suaempresa.com

# Limite de requisições por minuto por IP
MAX_REQUESTS_PER_MINUTE=10

# URLs permitidas para CORS (separadas por vírgula)
ALLOWED_ORIGINS=https://localhost:3000,http://localhost:3000,https://seudominio.com